'use client';

// Move all the existing code here
// Just change the component name and props interface
interface ClientContentProps {
  id: string;
}

export default function ClientContent({ id }: ClientContentProps) {
  // Copy all the existing code from page.tsx here
  // Replace all props.params.id with id
} 